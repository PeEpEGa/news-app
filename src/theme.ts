import { createTheme } from "@mui/material";

const theme = createTheme({
  //   palette: {
  //     background: {
  //       default: "#141414",
  //     },
  //   },

  typography: {},
});

export default theme;
